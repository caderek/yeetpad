import type { getRawMetadata } from "./getRawMetadata"

export function prepareMetadata(
  rawMetadata: ReturnType<typeof getRawMetadata>,
) {
  // choose favicon image title and descripton, maybe some more
}
